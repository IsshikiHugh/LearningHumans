from setuptools import setup, find_packages

setup(
    name         = "lib",
    version      = "1.0",
    author       = "IsshikiHugh",
    author_email = "",
    description  = "",
    packages     = find_packages()
)